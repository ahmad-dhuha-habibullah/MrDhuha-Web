---
layout: layouts/base.njk
---
<article class="container mx-auto px-4 lg:px-8 py-12 max-w-4xl">
    <!-- Breadcrumbs -->
    <div class="flex items-center gap-2 text-sm text-textLight mb-8">
        <a href="/" class="hover:text-primary">Beranda</a>
        <i data-lucide="chevron-right" class="w-4 h-4"></i>
        <a href="/artikel/" class="hover:text-primary">Artikel</a>
        <i data-lucide="chevron-right" class="w-4 h-4"></i>
        <span class="text-text truncate">{{ title }}</span>
    </div>

    <!-- Article Header -->
    <header class="mb-10 text-center md:text-left">
        {% if series %}
            <a href="/seri/{{ series | slugify }}/" class="inline-block bg-blue-100 text-blue-800 text-xs font-bold uppercase tracking-wider py-1 px-3 rounded-full mb-4 hover:bg-blue-200 transition-colors">Series: {{ series }}</a>
        {% elif topic %}
            <a href="/topik/{{ topic | slugify }}/" class="inline-block bg-orange-100 text-primary text-xs font-bold uppercase tracking-wider py-1 px-3 rounded-full mb-4 hover:bg-orange-200 transition-colors">{{ topic }}</a>
        {% endif %}
        <h1 class="text-3xl md:text-5xl font-extrabold text-text leading-tight mb-6">{{ title }}</h1>
        
        {% if type != 'page' %}
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 border-y border-border py-4">
            <div class="flex items-center gap-4">
                <div class="w-12 h-12 bg-gray-200 rounded-full overflow-hidden">
                    <img src="/assets/images/profile.jpg" alt="{{ author | default('Mr Dhuha') }}" class="w-full h-full object-cover">
                </div>
                <div class="text-left">
                    <a href="/tentang/" class="font-semibold text-text hover:text-primary transition-colors">{{ author | default('Mr Dhuha') }}</a>
                    <div class="text-sm text-textLight">{{ date | readableDate }} &middot; {{ readingTime | default('5') }} menit baca</div>
                </div>
            </div>
            
            <div class="flex gap-2">
                <button onclick="window.openShareModal()" class="flex items-center gap-2 px-3 py-2 text-sm font-medium text-text border border-border rounded-lg hover:bg-slate-100 hover:text-slate-900 dark:hover:bg-slate-700 dark:hover:text-white transition-colors">
                    <i data-lucide="share-2" class="w-4 h-4"></i> Bagikan
                </button>
                <button onclick="toggleBookmark('{{ page.url }}', '{{ title | escape }}')" class="flex items-center gap-2 px-3 py-2 text-sm font-medium text-text border border-border rounded-lg hover:bg-slate-100 hover:text-slate-900 dark:hover:bg-slate-700 dark:hover:text-white transition-colors">
                    <i data-lucide="bookmark" class="w-4 h-4" id="bookmarkIcon"></i> <span id="bookmarkText">Simpan</span>
                </button>
            </div>
        </div>
        {% endif %}
    </header>

    <!-- Featured Image -->
    {% if thumbnail %}
    <figure class="relative w-full mb-12">
        <div class="bg-slate-900 rounded-xl overflow-hidden shadow-lg">
            <img src="{{ thumbnail | resolveAsset(page.inputPath) }}" alt="{{ title }}" class="w-full object-cover max-h-[500px]">
        </div>
        {% if imageCaption %}
        <figcaption class="text-center text-sm text-textLight mt-3 font-serif italic">{{ imageCaption }}</figcaption>
        {% endif %}
    </figure>
    {% endif %}

    <!-- Article Body (Single Column per PRD) -->
    <div class="article-content max-w-3xl mx-auto">
        {{ content | safe }}
    </div>

    <!-- Series Navigation -->
    {% if series %}
        {% set prevInSeries = page | getPreviousInSeries(collections) %}
        {% set nextInSeries = page | getNextInSeries(collections) %}
        {% if prevInSeries or nextInSeries %}
        <div class="mt-16 pt-8 border-t border-border max-w-3xl mx-auto">
            <h3 class="font-bold text-lg mb-4 text-textLight uppercase tracking-wider text-center">Lebih Lanjut dalam Seri "{{ series }}"</h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {% if prevInSeries %}
                <a href="{{ prevInSeries.url }}" class="flex flex-col p-4 border border-border rounded-xl hover:bg-surface transition-colors text-left group">
                    <span class="text-xs font-bold text-textLight uppercase tracking-wider mb-2 flex items-center gap-1"><i data-lucide="chevron-left" class="w-3 h-3"></i> Sebelumnya</span>
                    <span class="font-semibold text-text group-hover:text-primary transition-colors leading-tight">{{ prevInSeries.data.title }}</span>
                </a>
                {% else %}
                <div></div>
                {% endif %}
                
                {% if nextInSeries %}
                <a href="{{ nextInSeries.url }}" class="flex flex-col p-4 border border-border rounded-xl hover:bg-surface transition-colors text-right group">
                    <span class="text-xs font-bold text-textLight uppercase tracking-wider mb-2 flex items-center justify-end gap-1">Selanjutnya <i data-lucide="chevron-right" class="w-3 h-3"></i></span>
                    <span class="font-semibold text-text group-hover:text-primary transition-colors leading-tight">{{ nextInSeries.data.title }}</span>
                </a>
                {% else %}
                <div></div>
                {% endif %}
            </div>
        </div>
        {% endif %}
    {% endif %}

    <!-- Comments and Reactions (Giscus) -->
    <div class="mt-16 pt-8 border-t border-border max-w-3xl mx-auto">
        <h3 class="font-bold text-2xl mb-6">Komentar & Diskusi</h3>
        <div class="giscus-container bg-surface border border-border p-6 rounded-2xl">
            <!-- INSERT GISCUS SCRIPT HERE -->
            <script src="https://giscus.app/client.js"
                    data-repo="ahmad-dhuha-habibullah/MrDhuha-Web"
                    data-repo-id="REPLACE_ME_REPO_ID"
                    data-category="General"
                    data-category-id="REPLACE_ME_CATEGORY_ID"
                    data-mapping="pathname"
                    data-strict="0"
                    data-reactions-enabled="1"
                    data-emit-metadata="0"
                    data-input-position="bottom"
                    data-theme="preferred_color_scheme"
                    data-lang="id"
                    crossorigin="anonymous"
                    async>
            </script>
        </div>
    </div>

    <!-- Related Articles / Related Videos -->
    <div class="mt-16 pt-8 border-t border-border max-w-3xl mx-auto">
        <h3 class="font-bold text-2xl mb-6">Baca Juga</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {% set relatedCount = 0 %}
            {% for item in collections.articles %}
                {% if item.url != page.url and relatedCount < 4 %}
                    <a href="{{ item.url }}" class="group flex gap-4 items-start">
                        {% if item.data.thumbnail %}
                        <img src="{{ item.data.thumbnail | resolveAsset(item.inputPath) }}" class="w-24 h-20 object-cover rounded-lg bg-gray-100 flex-shrink-0">
                        {% endif %}
                        <div>
                            <h4 class="text-base font-bold leading-tight group-hover:text-primary transition-colors mb-2">{{ item.data.title }}</h4>
                            <span class="text-sm text-textLight">{{ item.data.readingTime | default('5') }} menit baca</span>
                        </div>
                    </a>
                    {% set relatedCount = relatedCount + 1 %}
                {% endif %}
            {% endfor %}
        </div>
    </div>
</article>
